<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "ServiceFinder" %></title>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>

    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render 'layouts/application_nav' %>
    <div id="main" role="main" class="small-12 columns" data-turbolinks-temporary>
      <%#= render partial: 'layouts/flash' %>
      <div class="row">
        <%= yield %>
      </div>
      <div id="action_panel" class="reveal-modal" data-reveal aria-hidden="true" role="dialog"></div>
    </div>

    <%= render 'layouts/footer' %>
  </body>
  <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
</html>
